apiVersion: v1
kind: Service
metadata:
  name: {{ include "task7-nginx.fullname" . }}
  labels:
    {{- include "task7-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "task7-nginx.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
